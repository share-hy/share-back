spring:
  datasource:
    url: jdbc:mysql://localhost:3306/share?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: share-hy
    password: 123456
    druid:
      initial-size: 5
      min-idle: 10
      max-active: 20
      web-stat-filter:
        exclusions:
      stat-view-servlet:
        login-username: druid
        login-password: druid
  redis:
    host: localhost
    database: 0
    port: 6379
    password:
    timeout: 300ms

logging:
  file:
    path: /var/logs