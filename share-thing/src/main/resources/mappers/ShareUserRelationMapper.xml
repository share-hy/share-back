<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.share.hy.mapper.ShareUserRelationMapper">
  <resultMap id="BaseResultMap" type="com.share.hy.domain.ShareUserRelation">
    <!-- WARNING - @mbg.generated -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="first" jdbcType="VARCHAR" property="first" />
    <result column="second" jdbcType="VARCHAR" property="second" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  
  <sql id="BaseColumns">
    <!-- WARNING - @mbg.generated -->
    id, user_id, first, second, create_time, update_time
  </sql>
  <select id="countSubordinateGroupByLevel" resultType="java.util.Map">
    select
     (case
     when `first` = ${userId} THEN '1'
     when `second` = ${userId} THEN '2' end)
     as level,count(0) as `count`
        from share_user_relation
        where `first` = #{userId} or `second` = #{userId}
        group by `level`
  </select>

</mapper>